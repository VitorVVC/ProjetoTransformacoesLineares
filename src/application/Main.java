package application;


import java.util.Locale;

import static application.Util.*;

public class Main {
    public static void main(String[] args) {
        Locale.setDefault(Locale.US);

        // Trabalho Cenário DOIS ( AV2 ) --> Algebra Linear.

        // Puxa o método 'printSetado();' que nada mais é que o trabalho em um print.
        printSetado();

        // Puxa o método 'interacao();' que nada mais é que ter um contato o usuário que está testando o progama.
        //interacao();


    }
}